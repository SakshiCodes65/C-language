#include <stdio.h>
#include <string.h>

int main()
{
    char str1[] = "gol";
    char str2[] = "goel";
    printf("%d", strcmp(str1,str2));
    // if ans == 0 then it is equal.
    return 0;
}
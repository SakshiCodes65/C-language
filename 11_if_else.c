#include <stdio.h>

int main()
{
    int number = 200;
    if (number < 100)
    {
        printf("number is less than 100");
    }
    else
    {
        printf("number is not less than 100");
    }
    return 0;
}